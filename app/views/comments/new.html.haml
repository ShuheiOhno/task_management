コメントする
.comment
  = form_with model: @comment, local: true do |f|
    .comment_comment
      コメントする
      = f.text_area :comment
    .comment_image
      画像を選択する
      = f.file_field :image
    .comment_submit
      = f.submit "send"